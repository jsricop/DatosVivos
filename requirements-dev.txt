# Dependencias de desarrollo LOCAL — formato, linting, tests y el harness
# de evaluación (eval/run_eval_*.py). Ningún contenedor las instala.
#
#   pip install -r requirements-dev.txt

black>=24.0.0
ruff>=0.3.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0

# Harness de evaluación (goldens YAML + reportes en eval/reports/)
PyYAML>=6.0,<7.0
httpx>=0.27,<1.0
