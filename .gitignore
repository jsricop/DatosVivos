# Contexto interno
MAIN.md

# Environment
.env
*.env.local

# Data
data/
*.pkl
*.bin
*.index

# Python
__pycache__/
*.pyc
.venv/
venv/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Power BI temp
*.pbix.tmp

# Docker volumes
postgres_data/
ollama_models/

# Estado operativo personal — no se sube
STATUS.md

# Documentación pública (exigencia del concurso: repo replicable/auditable).
# Queda PRIVADO solo lo operativo: topología/credenciales de la VM.
/docs/etl_cron_devops.md
/CHANGELOG.md
/PLAN_DASHBOARD.md

# Lock files de Office (PowerPoint abierto)
~$*

# === Documentos de TRABAJO (se conservan localmente, no se publican) ===
# Bitácoras, handoffs y salidas regenerables: útiles para el desarrollo,
# exponen proceso interno sin aportar al lector del repo (ADR de depuración
# documental 2026-07-13).
docs/lessons_learned.md
docs/powerbi/HANDOFF_*.md
docs/powerbi/guia_powerbi.html
eval/ciudadano/entregado_ciclo*.yaml
eval/reports/*
# Excepción: la auditoría de calidad columna a columna SÍ se publica — es la
# evidencia de la afirmación "17/18 columnas al 100 %" que cita el README.
!eval/reports/data_quality_hito_q_2026-05-29.md

# === Poda documental máxima (2026-07-13): el repo público lleva código,
# README, BRAND e insumos del concurso. El resto de docs/ es material de
# trabajo interno — se conserva LOCALMENTE, no se publica.
docs/*
!docs/BRAND.md
eval/ciudadano/seguimiento_50.md
eval/ciudadano/analisis_ciclos.md

# Generador original del pitch — OBSOLETO como fuente (el pptx del equipo
# es la verdad desde 2026-07-13). Se conserva local como referencia; si se
# ejecutara, destruiría la edición manual.
scripts/gen_pitch.py
